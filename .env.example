# Supabase — in SQL Editor, run in order:
# 1) supabase/migrations/20260411210000_profiles_holdings_transactions_watchlist.sql
# 2) supabase/migrations/20260411220000_rename_profiles_level_if_exists.sql (only if an old profiles table had column "level")
VITE_SUPABASE_URL=https://puvklrwxhycacldogvoy.supabase.co/
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_LCAr1FCadr-AXDGPxKNqtw_Q6Aj_Bdr

# Finnhub — Python server only (never expose to Vite). No spaces/quotes around the key.
FINNHUB_API_KEY=

# Flask port — must match Vite’s /api proxy (default 5050).
# Do NOT use 5000 on macOS: AirPlay Receiver binds there and returns HTTP 403 to your API.
FLASK_PORT=5050

# Optional: production API origin only (no `/api`). Leave unset in local dev.
# VITE_API_URL=https://your-backend.example.com
